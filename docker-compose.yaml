version: '3.8'

services:
  carbon:
    build:
      context: .
      network: host
    restart: unless-stopped
    ports:
      - "8002:8000"
    volumes:
      - ./static:/app/static
      - ./data:/app/data
    env_file:
      - .env
    privileged: true