services:
  carbon:
    build: .
    restart: unless-stopped
    ports:
      - "8000:8000"
    volumes:
      - ./static:/app/static
      - ./data:/app/data
    env_file:
      - .env
    privileged: true